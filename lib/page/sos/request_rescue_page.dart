import 'package:flutter/material.dart';

class RequestRescuePage extends StatelessWidget {
  static String routeName="/request_rescue";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Request Rescue"),),
    );
  }
}
